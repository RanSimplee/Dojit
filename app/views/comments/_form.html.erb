<%= form_for [topic, post, post.comments.new] do |f| %>
  <div class="form-group">
    <%= f.label :new_comment %>
    <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter a comment" %>
  </div>
  <div class="form-group">
    <%= f.submit "Add Comment", class: 'btn btn-success' %>
  </div>
<% end %>